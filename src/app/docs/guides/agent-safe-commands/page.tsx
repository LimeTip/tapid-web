import AgentSafeCommands from "@/content/docs/guides/agent-safe-commands.mdx";

export const metadata = {
  title: "Agent-safe commands",
  description: "The package context an agent needs before running a command.",
};

export default function AgentSafeCommandsPage() {
  return <AgentSafeCommands />;
}
