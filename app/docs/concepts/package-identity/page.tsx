import PackageIdentity from "@/content/docs/concepts/package-identity.mdx";

export const metadata = {
  title: "Package identity",
  description: "How Tapid plans to identify an exact package artifact.",
};

export default function PackageIdentityPage() {
  return <PackageIdentity />;
}
