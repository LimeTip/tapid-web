import Commands from "@/content/docs/commands.mdx";

export const metadata = {
  title: "Commands",
  description: "The planned Tapid client command interface.",
};

export default function CommandsPage() {
  return <Commands />;
}
